# ═══════════════════════════════════════
# Multi-Model AI Bot 环境变量配置
# 复制此文件为 .env 并填入对应的 API Key
# 只需配置你要使用的提供商即可
# ═══════════════════════════════════════

# ─── OpenAI ───────────────────────────
OPENAI_API_KEY=your_openai_key
# OPENAI_BASE_URL=              # 可选，自定义 API 端点
# OPENAI_MODEL=gpt-4o           # 可选，默认模型

# ─── Anthropic Claude ─────────────────
ANTHROPIC_API_KEY=your_anthropic_key
# ANTHROPIC_BASE_URL=           # 可选，自定义 API 端点
# ANTHROPIC_MODEL=claude-sonnet-4-20250514  # 可选，默认模型

# ─── 阿里云百炼 (DashScope) ───────────
DASHSCOPE_API_KEY=your_dashscope_key
# DASHSCOPE_MODEL=qwen-plus     # 可选，默认模型

# ─── DeepSeek ─────────────────────────
DEEPSEEK_API_KEY=your_deepseek_key
# DEEPSEEK_MODEL=deepseek-chat  # 可选，默认模型

# ─── 月之暗面 (Moonshot/Kimi) ─────────
MOONSHOT_API_KEY=your_moonshot_key
# MOONSHOT_MODEL=moonshot-v1-8k # 可选，默认模型

# ─── 智谱 AI (ChatGLM) ───────────────
ZHIPU_API_KEY=your_zhipu_key
# ZHIPU_MODEL=glm-4-plus       # 可选，默认模型

# ─── 硅基流动 (SiliconFlow) ───────────
SILICONFLOW_API_KEY=your_siliconflow_key
# SILICONFLOW_MODEL=deepseek-ai/DeepSeek-V3  # 可选，默认模型

# ─── OpenRouter ───────────────────────
OPENROUTER_API_KEY=your_openrouter_key
# OPENROUTER_MODEL=openai/gpt-4o  # 可选，默认模型

# ─── Ollama (本地模型) ────────────────
# OLLAMA_ENABLED=true           # 设为 true 启用
# OLLAMA_BASE_URL=http://localhost:11434/v1  # 可选，Ollama 服务地址
# OLLAMA_MODEL=llama3           # 可选，默认模型
